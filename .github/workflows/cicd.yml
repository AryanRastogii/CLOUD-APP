# name: cloud-app depploy using github actions

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code app
#         uses: actions/checkout@v4

#       - name: Deploy via ssh
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ubuntu
#           key: ${{ secrets.SSH_KEY }}
#           script: |
#             cd /home/ubuntu/CLOUD-APP
#             git pull origin main
#             docker compose down
#             docker compose up -d --build

name: cloud-app deploy using github actions

on:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code app
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/ubuntu/CLOUD-APP

            git pull origin main

            docker compose down

            docker compose up -d --build